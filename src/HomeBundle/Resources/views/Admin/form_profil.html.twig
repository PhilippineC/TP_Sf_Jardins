{{ form_start(form, {'attr': {'class': 'form-horizontal'}},
{'action': path('admin_add')}) }}

        {# Les erreurs générales du formulaire. #}
        {{ form_errors(form) }}

<div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.nom, "Nom", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.nom) }}
    <div class="col-sm-6">
        {# Génération de l'input. #}
        {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.metier, "Métier", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.metier) }}
    <div class="col-sm-6">
        {# Génération de l'input. #}
        {{ form_widget(form.metier, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.biographie, "Biographie", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.biographie) }}
    <div class="col-sm-6">
        {# Génération de l'input. #}
        {{ form_widget(form.biographie, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.twitter, "Compte Twitter", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.twitter) }}
    <div class="col-sm-6">
        {# Génération de l'input. #}
        {{ form_widget(form.twitter, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.facebook, "Compte Facebook", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.facebook) }}
    <div class="col-sm-6">
        {# Génération de l'input. #}
        {{ form_widget(form.facebook, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.linkedin, "Compte Linkedin", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    {{ form_errors(form.linkedin) }}
    <div class="col-sm-6">
        {# Génération de l'input. #}
        {{ form_widget(form.linkedin, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>

<div class="form-group">
    {{ form_label(form.file, "Photo", {'label_attr': {'class': 'col-sm-3 control-label'}}) }}
    {{ form_errors(form.file) }}
    <div class="col-sm-6">
        {% if profil.photo is defined %}
            <img src="{{ asset(profil.photomin)}}"/>
        {% endif %}
        {{ form_widget(form.file) }}
    </div>
</div>

<div class="btn col-sm-offset-5">
    <a href="{{ path('admin') }}" class="btn btn-default">
        <i class="glyphicon glyphicon-chevron-left"></i>
        Retour à l'espace d'administration
    </a>
    {{ form_widget(form.enregistrer, {'attr': {'class': 'btn btn-primary'}}) }}
</div>

{# Génération du label + error + widget pour un champ #}


        {# Génération automatique des champs pas encore écrits.
           Dans cet exemple, ce serait le champ CSRF (géré automatiquement par Symfony !)
           et tous les champs cachés (type « hidden »). #}

        {{ form_rest(form) }}

        {# Fermeture de la balise <form> du formulaire HTML #}
        {{ form_end(form) }}